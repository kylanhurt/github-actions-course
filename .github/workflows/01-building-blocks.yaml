name: 01 - Building Blocks
  on: [push, workflow_dispatch]

jobs:
  name: echo-hello
    runs-on: ubuntu-latest
    steps:
      - name: Echo Hello
        run: echo "Hello"
  name: echo-goodbye
    runs-on: ubuntu-latest
    steps:
      name: Failed step
        run: echo "I will fail?" && exit 1
      name: Say Goodbye
        run: echo "Goodbye!"